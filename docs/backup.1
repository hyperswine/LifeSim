    // resize matrix so it is a x b

    // if increase rows, then append 0s
    // if(a > rows()){
    //     CMatrix temp(a, b);
    // }
    // if decrease rows, then truncate
    // else{

    // }
    // if increase cols, then append 0s
    // if(b > cols()){

    // }
    // if decrease cols, then truncate
    // else{

    // }

std::ostringstream _stream;
    va_list ap;
    va_start(ap, x);

    while(true){
        std::any _to = va_arg(ap, std::any);
        if ()
        _stream << _to;
        std::cout << _to;
    }
